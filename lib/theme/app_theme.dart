import 'package:moor_lists/theme/app_colors.dart';
import 'package:moor_lists/theme/app_spacing.dart';

class AppTheme {
  static AppColors colors = AppColors();
  static const AppSpacing spacing = AppSpacing();
}
