import 'package:flutter/widgets.dart';

class AppSpacing {
  const AppSpacing();

  final EdgeInsets taskCardPadding = const EdgeInsets.all(5.0);
  final EdgeInsets textBoxMargin = const EdgeInsets.all(4.0);
}
